1.
``` sql
create table NAMETABLE(
  id number primary key, 
  name varchar(256)
  
  
  foreign key()
        references 
        on delete cascade,
    
  primary key(,)
);
```

``` sql
create table NAMETABLE(
  id number primary key, 
  name varchar(256)
  
  
  foreign key()
        references 
        on delete cascade,
    
  primary key(,)
);
```

``` sql
create table NAMETABLE(
  id number primary key, 
  name varchar(256)
  
  
  foreign key()
        references 
        on delete cascade,
    
  primary key(,)
);
```

``` sql
create table NAMETABLE(
  id number primary key, 
  name varchar(256)
  
  
  foreign key()
        references 
        on delete cascade,
    
  primary key(,)
);
```

``` sql
create table NAMETABLE(
  id number primary key, 
  name varchar(256)
  
  
  foreign key()
        references 
        on delete cascade,
    
  primary key(,)
);
```

``` sql
create table NAMETABLE(
  id number primary key, 
  name varchar(256)
  
  
  foreign key()
        references 
        on delete cascade,
    
  primary key(,)
);
```
